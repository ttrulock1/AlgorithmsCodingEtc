//Math.max= you put to parameters in there and it takes the larger number. ex.
let counter = 0
let MaxConsecutive = 0

let maxConsecutive = Math.max(MaxConsecutive,counter)

// Math.floor seems to add numbers together.
Math.floor((high + low)/2)

